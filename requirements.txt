# Core ML — torch and torchvision are installed separately from
# PyTorch's nightly index (CUDA 13.0) by foxburrow.sh. Do NOT move
# them into this file — pip's resolver will conflict with the nightly
# pre-release versions when resolving other packages.
diffusers>=0.27
transformers>=4.40
safetensors>=0.4
accelerate>=0.27

# LoRA adapter loading (required by diffusers for load_lora_adapter)
peft>=0.18

# Tagger (JTP PILOT2 SigLIP)
timm>=1.0

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# GPU monitoring (provides pynvml)
nvidia-ml-py>=12.535

# cuDNN workspace profiling (predicts VRAM usage before execution)
nvidia-cudnn-frontend>=1.0

# Background removal (RMBG-2.0 dependency)
kornia>=0.7

# TUI console
textual>=0.50

# Checkpoint metadata (companion YAML sidecar detection)
PyYAML>=6.0

# Image / numeric
Pillow>=10.0
numpy>=1.24
